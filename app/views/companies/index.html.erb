<p id="notice"><%= notice %></p>
<div class="row">
  <div class="col-4">

  </div>
  <div class="col-5">
    <div class="card index">
      <h1 class="text-center">Compañias</h1>

      <table class="ml-3">
        <thead>
          <tr>
            <th>Nombre</th>
            <th>Cantidad</th>
            <th colspan="3"></th>
          </tr>
        </thead>

        <hr>
          <tbody>
              <% @companies.each do |company| %>
              <tr>
                <td><%= link_to company.name, company %></td>
                <td><%= company.employees.count %></td>
                <td><%= link_to 'Editar', edit_company_path(company), class:'btn btn-dark' %></td>
                <td><%= link_to 'Eliminar', company, method: :delete, data: { confirm: 'Are you sure?' }, class:'btn btn-dark' %></td>
              </tr>
              <% end %>
          </tbody>
      </table>
      <hr>
      <br>
      <div class="container">
        <hd class="abajo"><%= link_to 'Nueva Empresa', new_company_path, class:'btn btn-dark' %>
      </div>
  </div>
</div>
